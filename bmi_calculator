def bmi_calculator():
    weight = float (input ("Wie viel wiegst du in kg?:"))
    height = float (input ("Wie groß bist du in m?:"))
                
    height = height/100
    bmi = weight / (height * height)
    if (bmi>0):
        if (bmi<=19):
         print ("Du bist untergewichtig")
        else:(bmi<=20)
        print ("Du bist im normalen Bereich")
    else: ("Bitte gib gültige Mase an")
    print (f"Dein BMI ist {bmi}")
bmi_calculator ()
